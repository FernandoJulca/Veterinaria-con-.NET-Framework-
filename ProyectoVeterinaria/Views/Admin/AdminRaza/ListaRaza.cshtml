@model IEnumerable<Dominio.Entidad.Entidad.Raza>

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewBag.Menu = "Razas";
    ViewBag.Title = "Lista de Razas";
}

<div class="d-flex justify-content-between mb-3">
    <div aria-label="Paginación de Razas" class="mt-3">
        <div class="pagination justify-content-center gap-1">
            @for (int i = 1; i <= ViewBag.TotalPages; i++)
            {
                <button type="button"
                        class="btn btn-sm mx-1 btn-pagina button-paginado @(i == ViewBag.CurrentPage ? "pagina-activa" : "")"
                        onclick="window.location='@Url.Action("ListaRaza", new { page = i })'">
                    @i
                </button>
            }
        </div>
    </div>
    <div class="d-flex align-items-center">
        <a href="@Url.Action("Create")" class="btn btn-outline-success">
            <i class="fas fa-paw"></i> Agregar Raza
        </a>
    </div>
</div>

<div class="table-responsive">
    <table class="table table-hover table-bordered align-middle shadow-sm rounded">
        <thead class="table-success text-center">
            <tr>
                <th><i class="fas fa-hashtag"></i><br />@Html.DisplayNameFor(model => model.First().IdRaza)</th>
                <th><i class="fas fa-paw"></i><br />@Html.DisplayNameFor(model => model.First().NombreRaza)</th>
                <th><i class="fas fa-paw"></i><br />@Html.DisplayNameFor(model => model.First().NombreEspecie)</th>
                <th><i class="fas fa-cogs"></i><br />Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="text-center">@item.IdRaza</td>
                    <td>@item.NombreRaza</td>
                    <td>@item.NombreEspecie</td>
                    <td class="text-center">
                        <a href="@Url.Action("Edit", new { id = item.IdRaza })" class="btn btn-sm btn-outline-warning me-1" title="Editar">
                            <i class="fas fa-edit"></i>
                        </a>
                        <a href="@Url.Action("Details", new { id = item.IdRaza })" class="btn btn-sm btn-outline-info me-1" title="Detalles">
                            <i class="fas fa-info-circle"></i>
                        </a>
                        <a href="@Url.Action("Delete", new { id = item.IdRaza })" class="btn btn-sm btn-outline-danger" title="Eliminar">
                            <i class="fas fa-trash-alt"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
