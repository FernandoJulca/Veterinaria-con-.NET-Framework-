@model Dominio.Entidad.Entidad.Raza

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewBag.Menu = "Razas";
    ViewBag.Title = "Detalle de Raza";
}

<h2 class="mb-4"><i class="fas fa-info-circle text-info"></i> Detalle de Raza</h2>

<div class="card shadow-sm border-0">
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-4 fw-bold text-secondary">
                @Html.DisplayNameFor(model => model.IdRaza)
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(model => model.IdRaza)
            </dd>

            <dt class="col-sm-4 fw-bold text-secondary">
                @Html.DisplayNameFor(model => model.NombreRaza)
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(model => model.NombreRaza)
            </dd>
            <dt class="col-sm-4 fw-bold text-secondary">
                @Html.DisplayNameFor(model => model.NombreEspecie)
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(model => model.NombreEspecie)
            </dd>
        </dl>
    </div>

    <div class="card-footer bg-white text-end">
        <a href="@Url.Action("Edit", new { id = Model.IdRaza })" class="btn btn-outline-warning me-2">
            <i class="fas fa-edit"></i> Editar
        </a>
        <a href="@Url.Action("ListaRaza")" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Volver
        </a>
    </div>
</div>
