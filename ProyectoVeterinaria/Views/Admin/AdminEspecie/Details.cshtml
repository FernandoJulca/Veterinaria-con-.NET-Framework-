@model Dominio.Entidad.Entidad.Especie

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewBag.Menu = "Especies";
    ViewBag.Title = "Detalle de Especie";
}

<h2 class="mb-4"><i class="fas fa-info-circle text-info"></i> Detalle de Especie</h2>

<div class="card shadow-sm border-0">
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-4 fw-bold text-secondary">
                @Html.DisplayNameFor(model => model.IdEspecie)
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(model => model.IdEspecie)
            </dd>

            <dt class="col-sm-4 fw-bold text-secondary">
                @Html.DisplayNameFor(model => model.NombreEspecie)
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(model => model.NombreEspecie)
            </dd>
        </dl>
    </div>

    <div class="card-footer bg-white text-end">
        <a href="@Url.Action("Edit", new { id = Model.IdEspecie })" class="btn btn-outline-warning me-2">
            <i class="fas fa-edit"></i> Editar
        </a>
        <a href="@Url.Action("ListaEspecie")" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Volver
        </a>
    </div>
</div>
